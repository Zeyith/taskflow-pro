export const appRoutes = {
  home: '/',
  login: '/login',
  dashboard: '/dashboard',
  projects: '/projects',
  notifications: '/notifications',
  incidents: '/incidents',
  register: '/register',
} as const;